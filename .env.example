# Optional email delivery. Configure these as hosted environment values, never in source.
RESEND_API_KEY=
EMAIL_FROM=
# Optional authenticated scheduler for POST /api/email-jobs. Use a long random secret.
EMAIL_JOB_SECRET=
